{
    "image": "galacticusorg/buildenv:latest",
    "containerEnv": {
	"LC_ALL": "C",
	"GALACTICUS_EXEC_PATH": "/workspaces/galacticus",
	"GALACTICUS_DATA_PATH": "/workspaces/datasets",
    },
    "onCreateCommand": "apt-get update && apt-get install -y emacs && git clone git@github.com:galacticusorg/datasets.git /workspaces/datasets",
    "updateContentCommand": "git -C /workspaces/datasets pull",
}
